name: Deploy payment Worker

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "reduniq-worker/**"
      - ".github/workflows/deploy-payment-worker.yml"

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - name: Verify production payment configuration
        shell: bash
        run: |
          if ! grep -q '"REDUNIQ_ENVIRONMENT": "production"' reduniq-worker/wrangler.jsonc; then
            echo "REDUNIQ must remain on the confirmed production environment."
            exit 1
          fi
          if ! grep -q '"REDUNIQ_INTEGRATION_MODE": "api-gateway"' reduniq-worker/wrangler.jsonc; then
            echo "The verified API Gateway integration must remain selected."
            exit 1
          fi
          if ! grep -q '"PAYMENTS_ENABLED": "true"' reduniq-worker/wrangler.jsonc || ! grep -q '"COMMERCE_ENABLED": "true"' reduniq-worker/wrangler.jsonc; then
            echo "The production payment and commerce switches must remain explicit."
            exit 1
          fi
      - name: Verify Cloudflare deployment credentials
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          if [ -z "$CLOUDFLARE_API_TOKEN" ] || [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
            echo "Required Cloudflare repository secrets are not installed."
            exit 1
          fi
      - name: Apply production hardening migration
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: reduniq-worker
          command: d1 migrations apply rival-praxis-invoices --remote
      - name: Deploy Worker and custom domain
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: reduniq-worker
          command: deploy
